# HANDOFF — 클라우드 세션 → 로컬 세션 인수인계

작성: 2026-08-25, 클라우드 세션 종료 시점. 기준 커밋 main @ `0cdc3cc`(+이 문서 커밋).
갱신: 2026-08-25, 로컬 세션 — AGENTS.md 계층 문서화 완료 반영(§2-7, §5 ELI5 해결).
기준 커밋 main @ `b7315fd`.
갱신 2: 2026-08-25 — 가이드 1·2부 콘텐츠 전면 교정(§2-8, §5 wrap 잔존 11→3).
갱신 3: 2026-08-25 — CAPE 실측 연장: Shiller 원본 직변환으로 소스 교체(§2-9).
갱신 4: 2026-08-25 — CAPE 기준값 통일(에피소드 고점월 실측 22.3/32.6/42.9) +
신선도 가드. §5의 1968=24.1 불일치 발견은 해결 처리.
저장소 상태: clean, 전부 푸시·배포 완료(Vercel READY). 테스트 170개 통과.
표준 규칙·검증 워크플로·아키텍처 지도는 루트 `CLAUDE.md` 참조. 디렉토리별 상세
지침은 각 디렉토리의 `AGENTS.md`(13개 계층 — 루트 AGENTS.md에서 시작). 이 문서는
그보다 깊은 이력·규약·미결 과제를 담는다.

---

## 1. 프로젝트 한 줄 지도

"투자의 정석" = 교육 3면(가이드북 1·2부, 역사 연구, 현재 신호) + 도구 1면(백테스터)
+ 홈 콕핏. 백엔드 없음(정적 SPA + Cloudflare/Vercel 함수 프록시 `functions/yf`,
`functions/fred`). 데이터는 야후 v8 일별 + 번들(`public/data/history.json`,
1900~ 월간 실질 총수익·매크로, `scripts/build-history.mjs`로 생성).

- 배포: main 푸시 → Vercel 자동. projectId `prj_Z5KJWVV9ZL1qzgzxeuj8PRn8nECy`,
  teamId `team_qKUDEGSTI3XqjH9aOJkfcASf` (배포 확인용).
- localStorage 키: `bt_theme`, `bt_strategies_v1`, `bt_shared_v1`,
  `bt_guide_progress_v1`(가이드 학습 진도 — guideProgress.ts와 HomeView가 공유).

## 2. 최근 작업 타임라인 (오래된 → 최신)

1. **백테스터 세밀화**: 전략별 낙폭 대응 규칙(기준 2종: 전고점=growth-of-$1 역대
   최고점 / 투입원금=누적 납입 대비), 규칙 발동 구간 `ruleEpisodes`를 결과 차트에
   전략색 음영으로 표시, 문장형 규칙 편집기+자동 요약문, 기간별 월 적립 조정
   (`contributionOverrides`, 차트 브러시 드래그로 추가), 보고서(PDF)에 규칙·조정 표기.
2. **정밀 감사**: 수익률 왜곡 버그 수정 — 선두 결측 forward-fill, ^IRX 0/음수 허용,
   세금-낙폭 1스텝 소급 보정, -HIST×크립토 혼합 차단, 정수주 강제매도 ceil,
   fixed_split stray key 검증, 시작월 월 적립(i===0) 누락. 골든 테스트로 고정.
3. **문체 대공사**: em-dash 721→196(잔존은 제목·결측·보호 인용), 이후 번역투·
   AI 문체 정밀 점검(567블록 스캔, 12건 교정 — 대명사 직역, 메타 신호어,
   근접 반복 종결). "~인 이유입니다"는 시그니처로 유지하되 근접 반복만 축소.
4. **가이드 전면 개편**:
   - 도해 인프라 `GuideFigure`(bars 부호 지원/flow/quad, HTML·CSS 기반 — SVG 텍스트
     금지가 의도), 1부 8곳·2부 13곳 = 21개 배치. `GuideSection.figure?` 필드.
   - 인사이트 2절 신설: `tg-m5-limits`(적립 방패의 유통기한 — 월 적립÷평가액 비율
     소멸, 글라이드패스·시퀀스 리스크), `tg-c-why`(공부의 배당 네 계좌).
   - 출판 편집자 관점 리뷰 반영: 지침서 내부 "1부/2부" 표기를 STEP 표기로 통일
     (탭 파트 명칭과 충돌 해소), tg-s0 완충문+네 관문 flow, 전방 참조 표지,
     BEI·기축통화·GDP 계단, 2부 심화 절 우회 안내 등.
   - 1부 용어 사전 24항목 신설(`TRADING_GUIDE_GLOSSARY`), GuideView 사전 렌더를
     파트별 데이터로 일반화.
   - QT 서술 최신화: "2차 QT 2022~2025년 말 종료, 약 9조→6조 중반" (사용자 승인,
     유지할 것).
5. **UI 3안 시안 → 전면 구현** (디자인 캔버스 아티팩트:
   https://claude.ai/code/artifact/35d080e5-ab6b-4efe-8ead-30408fa9297e)
   - **A 시그널 콕핏**: 홈 뷰 신설(기본 뷰), 체제 히어로+신호 5종 미니카드+이어서
     3카드. 내비 개편: 상단 탭 제거, lg+ 좌측 아이콘 레일, 모바일 하단 탭바.
     헤더는 로고(홈 이동)+액션 아이콘만.
   - **B 학습 여정**: 가이드 진도 자동 기록(IntersectionObserver로 절 도달 시),
     진도 링 히어로·이어 읽기·챕터 완료 체크·다음 챕터 내비. `guideProgress.ts`.
   - **C 워크벤치**: 백테스트 lg+ 분할(전략 스택 400px | 결과 캔버스), 모바일
     3단계 위저드(전략→가정→결과, 실행 성공 시 자동 3단계), 기간 프리셋 칩
     (전체/닷컴/금융위기/2022). 차트는 기존 표기(선형) 유지 — 로그 금지(사용자 지시).
6. **전수 검수 2회**:
   - 오버플로: 5뷰×3뷰포트(390/1280/1440) 자동 검사 0건.
   - 줄바꿈 품질: Range API 검출기로 "공백 있는 괄호가 두 줄에 걸침" 60건 검출
     → 49건 해소, 11건 허용(22자+ 절 단위 괄호의 본문 내 줄바꿈 — 표준 조판).
     해소 수단: `nbspShortParens` 상한별 적용(본문 rich 18자, 표 셀 20자, 용어
     사전 22자, 도해 라벨·격자 12자, 도해 캡션 20자), 막대 도해 모바일 라벨
     전폭 적층, 표 헤더 줄바꿈 허용+모바일 패딩 축소, 문구 3곳 단순화.
   - 헤더 현재 위치 라벨 제거, 푸터 저작권(© 연도 자동 갱신, 김현성) 추가.
7. **AGENTS.md 계층 문서화** (2026-08-25, 로컬 세션, `b7315fd`): 전 디렉토리에
   13개 생성 — 목적·주요 파일·AI 에이전트 지침·의존 관계. 도메인 규약(엔진 하루
   순서 8단계, 동기화 의무 3종, 데이터 재생성 시퀀스, nbspShortParens 상한,
   localStorage 키 인벤토리)을 해당 디렉토리 지침으로 반영. 단일 파일 디렉토리
   (functions/{yf,fred,stooq}, src/hooks, docs/{guides,research}, data-src/verify,
   public/data)는 부모 문서에 통합. 부수: `.gitignore`에 `.omc/` 추가,
   deepinit 매니페스트 저장(증분 재생성용, Desktop 워크스페이스 `.omc/`).
8. **가이드 1·2부 콘텐츠 전면 교정** (2026-08-25, 로컬 세션): 3-lane 리뷰
   (AI 문체 / 학습자 오개념 / 구조) 반영 + 사용자 지적 "독자 관점" 교정.
   - 커브 절(s2-curveshapes) 개편: (심화) 표기+우회 안내, 4국면 **양면 해석 병기**
     (2004~06 그린스펀·2013 테이퍼·2022~24 해소 — 전부 본문 기존 수치 재활용),
     불/베어(채권 가격 기준)×주도 만기 통합 암기 규칙, quad 셀에 주도 만기 표기,
     STEP 3 goal을 카드 실제 어휘("장단기 금리차 역전")로 교정.
   - 문체(AI 티 해소): 은유 적층 제거 — 자→층→펌프(sl-what+사전), 바늘·삼형제
     (si-map), 체온계(신용 스프레드 3곳), 여정·사다리·지도(제목), 닻·눈금(sf-fx+사전),
     '~하는 자' 제목 3연작 자연화, "정직한+명사" 5건 변주, 근접 반복 종결 완화.
   - **독자 관점(사용자 지적)**: "(N단계)" 포인터 괄호 약 30곳 삭제 또는 자기완결
     리라이트. 의도적 잔존 3곳뿐 — 인트로의 구조 설명, s2-mechanics 심화 우회 안내,
     STEP 4 도입의 회수 앵커(신설). 먼 "①번 뜻" 참조는 인라인 재서술.
   - 내용: s2-bond "이미 보유한(특히 장기) 채권" 한정+신규 매수 반대면, 2022 성적
     "연간 기준" 라벨(1부 월평균 병기와 이중 기준 해소), 불태화 해설(보호 인용
     원문 불변·reading만), 트레일링 P/E 정의, CAPE 1968=24.1 표기 통일(보호 인용
     기준), 10y−2y 관행 병기, 묶음 규칙 단정 완화+반례 인라인, tg-t1 명목 라벨.
   - 구조: s2-policy에서 한국(금통위) 절 분리(**s2-policy-kr 신설**, 사전 앵커 이동),
     tg-s3에 10회 vs 7구간 기준 해설(원문 md에서 역이식).
   - 3중 동기화: trading-discipline.md 2곳(명목 라벨·"덧붙일 주의") 동반 수정.
     verify-timing-math.mjs는 수치 무변경이라 해당 없음.
   - 검증: 테스트 170개 통과(앵커 전부 보존), 오버플로 0건, 괄호 줄바꿈 잔존
     11→3건(FactSet 나열·now 카드 부연·기준금리 사전 — 전부 기존 허용 계열, 신규 0).
   - **보류(수정 제외, 재확인 필요)**: FactSet 계열 수치, 실현 P/E 192/12 검증 이력,
     Barber&Odean 표본 기간 명시, 커브 형태 특성화 3건(1995년형 등), "N의 감각:"
     리드인(의도 여부 사용자 확인), 1부 실증 ③ 재배치(원문 md 개정 시 동반).
9. **CAPE 실측 연장 — Shiller 원본 직변환으로 소스 교체** (2026-08-25, 로컬 세션):
   사용자 지적("주가 변화만으로 연장한 프록시가 유효한가")에서 출발한 확장안 실행.
   - 배경: 기존 소스(github/datasets 미러)는 펀더멘털이 2023-06에서 갱신 중단 →
     프록시가 3년 치를 연장하며 실측 대비 약 +9% 과대(프록시 44.5 vs 공표 40.6, 2026-07).
   - 조치: 원본 ie_data.xls(shillerdata.com, 저장본 2026-08-04)를 동일 스키마 CSV로
     직변환해 교체. 컬럼 최신: P·CAPE 2026-08(41.18) / D 2026-06 / E 2026-03.
     FRED 4계열·TIPS·금 CSV도 최신화(2026-07). 프록시는 이제 배당 미확정 꼬리
     1~2개월만 담당. dataEnd 2026-05 → **2026-07**, 번들 1519개월.
   - 검증: 빌드 앵커 전부 통과, 에피소드 7개 수치 완전 동일(콘텐츠 앵커 무영향),
     verify-history 18체크 통과, 테스트 170개(1974-07 원본 오기입 79.31 재확인 —
     교정 유지, peFwdReal 경계 앵커는 불변식 형태로 강화), E2E 오버플로 0·wrap 3.
   - UI: 신호 카드 프록시 설명문·NowView 각주·가이드 s3-cape(예시 44.5→41.2)·
     어닝일드 예시(44/2.3→41/2.4)·광기의 해부 편차 문장을 실측 체계로 갱신.
     카드 reason 템플릿의 조사 오류("44.5은")도 괄호 구조로 해소(보호 인용 동시 수정).
   - 재현: scratchpad에서 xlsx(SheetJS)로 Data 시트 파싱 → 소수 5자리 반올림,
     결측 0.0, 날짜 YYYY-MM-01 (원본 소수년 표기 주의: .1 = 10월).
   - **후속 2(같은 날) — 연대기 "흐름 따라가기" 재구성(확장안 구현, §5 진단 해소)**:
     ① 국면 선택 시 상세 차트가 **국면 ±6개월로 자동 확대 + 자산은 국면 시작=100
     재정규화**(확대 배너에 "전체 구간 보기" 토글, 툴팁·제목·라벨 연동) ② lg+에서
     **연대기(좌)|차트(우, sticky top-20) 2열**, 모바일은 활성 카드에 **인라인 미니
     스파크**(lg:hidden) ③ 활성 카드에 **"자동 실측" 줄** — 국면 구간의 자산 4종
     변화율+CPI·금리·CAPE 이동을 번들에서 즉석 계산(수기 '데이터:' 줄과 상호 검증)
     ④ **이벤트 마커**(eraTimelines에 ERA_MARKERS 신설, 7구간 4~7개 — 연대기 본문에
     명시된 날짜만, 확대 모드에서 세로 점선+라벨) ⑤ **"일별 확대 보기"**(1928-01
     이후 창 한정): ^GSPC 일별을 /yf로 조회해 120px 스트립으로 표시 — 명목·배당
     제외임을 캡션에 명시, historyExtra.fetchGspcDailyWindow(메모리 캐시).
     국면 이동은 changePhase() 단일 통로(확대 리셋·일별 끔). 테스트 171개(마커
     검증 신설), 오버플로 0, wrap 신규 0. Playwright 헤드리스로 PC·모바일 실캡처
     검증(브라우저 팬 캡처가 이 세션에서 먹통이라 대체 — DOM 검증은 팬으로 완료).
   - **후속(같은 날) — CAPE 기준값 통일 + 신선도 가드**: "1968년 24.1" 앵커를
     "에피소드 고점월의 실측 CAPE" 기준으로 통일 — **1968-12 22.3 · 1929-09 32.6 ·
     2000-08 42.9** (판정 임계 24→22.3, 44→42.9. CAPE 사상 최고 1999-12의 44.2는
     별도 표기 유지). 파생 정정: "1996년 이후 4년 더" → "1995년 이후 5년 더"(교차월
     1995-06 재계산). 적용: nowSignals 원문·임계·아날로그, NowView 기준선, 가이드
     보호 인용+본문 3곳+도해(막대 재배열: 현재 41.2가 사상 최고 44.2 아래), 연구
     문서, 테스트. 내구성 가드 2종: ① 카드 프록시 문구가 실측 기준월을 동적 표기
     ② 빌드에 실측 공백 >4개월 콘솔 경고. wrap 잔존 3→2건(now 카드 건 자연 해소).
10. **역사 탭 모바일 UX 종합 개선** (2026-08-25, 로컬 세션): /review "모바일 UI
    직관성(특히 역사 탭)" → Playwright 390px 계측 진단 + 독립 리뷰어 2차 검증 →
    사용자 "확장안 모두 적용" 지시로 구현.
    - ① **시대 프리셋 칩 5종**(전체/대공황/스태그플레이션/닷컴·금융위기/최근 20년,
      워크벤치 기간 칩 패턴 재사용) — 9px 브러시 손잡이의 터치 한계 보완. 비제어
      Brush와는 brushEpoch 재마운트+start/endIndex로 동기화, 수동 드래그 시 칩 해제.
      Brush height 34→44, traveller 8→14.
    - ② **국면 하단 내비**(활성 카드 아래 [← 이전 국면][다음 국면 →][큰 차트
      보기 ↑]) + 확대 배너에 [연대기로 ↓] — 모바일에서 차트↔연대기 왕복 스크롤
      (~2,800px) 제거. 상단 국면 내비·액션 버튼 py-2 통일(터치 타깃 27→35px+).
    - ③ 자산 차트 **전체 모드 로그 축**+고점=100 기준선+캡션(국면 확대 시 자동
      선형 복귀) — 금 +1,100% 스케일에 눌려 주식 -50%가 안 보이던 문제.
    - ④ **매크로 이중축 분리**: <lg에서 CAPE를 우축 대신 별도 110px 스트립으로
      (340px 폭 잠식 완화), 캡션 분기. ⑤ 상세 3차트 툴팁 단위 중복 제거
      (formatter는 값만, labelFormatter에 "· 단위"). ⑥ HelpTip 히트 영역
      14→30px(p-2 -m-2, 시각 원 14px 유지). ⑦ 스파크 캡션을 색 범례로.
    - ⑧ 리뷰어 발견 버그 픽스: 시대 카드 재클릭 시 changePhase(null) 누락으로
      이전 국면 상태가 새 시대에 누수되던 문제(개요 밴드 경로 포함).
    - ⑨ **유령 툴팁 가드**: 구간 선택의 자동 스크롤로 차트가 탭 좌표 아래에 들어와
      툴팁이 저절로 뜨던 문제. recharts v3는 Tooltip trigger prop을 무시하고(실측)
      좌표 기반이라 pointer-events 차단만으론 부족. **주의(같은 날 재작업)**: 최초
      구현(차트 밖 touchstart마다 tooltipEpoch 재마운트)은 touchstart~click 사이
      DOM 변경으로 **연대기 카드·하단 내비의 첫 탭이 무시되는 회귀**를 일으켜
      (사용자 실기기 보고) 전면 교체 — 재마운트 없이 coarse에서 차트 밖 터치 시
      루트에 `[&_.recharts-tooltip-wrapper]:hidden` 클래스만 토글(차트 위 터치 시
      해제). 같은 값 setState는 리렌더 없음. inert/pointer-events 가드도 불필요해져
      제거. 가시성 기준 실측: 선택 직후·정착 후 유령 없음 / 탭 시 표시 / 밖 탭 시
      해제 / 시대·국면·내비 전부 단일 탭 반응 + 탭 시 차트 재마운트 없음.
    - 검증: 테스트 171·빌드·오버플로 0·wrap 신규 0(잔존 2), Playwright 모바일
      계측으로 전 항목 작동 확인. 데스크톱 lg 2열 레이아웃 변화 없음.

11. **가이드 1·2부 AI 문체 전수 재점검 반영** (2026-08-27, 2d59e6a 외):
    - 독립 리뷰어 전수 통독 + 기계 grep 이중 점검 → 원문 대조 검증 후 적용.
      확정 18건(압축체 3, "흥미롭게도" 1, '자' 라벨 4, "이는" 4, 직역 칼크 2,
      미정의 "프리미엄"·"서열 정보" 2, "~의 함수" 4, 4연속 어미 사슬 1) +
      경계 10곳('눈금'→'기준선' 9곳 통일, 어미 사슬 4곳 완화, 런인 소제목
      대시 6곳 괄호형 통일, "세 문장" 선참조 해소, "비교 대상 교정"→"바꾸기").
      PER 월세 비유(GC:643)는 리듬 의도로 의도적 유지. 보호 인용(quotes)
      내부의 대시는 규칙(원문 예외)대로 유지. trading-discipline.md 동일
      문장 2곳 동기화. 관련 문답 교정 이력: 어닝일드 대목은 착시 방향
      오류(고물가기="비싸" 보임이 맞음)와 2.3→2.4% 통일 후, 추상 압축체
      재지적을 받아 쉬운 화법으로 재작성(e1b0408→caeaa20→e8f2528).
    - **주의**: 2d59e6a 커밋 메시지의 "타이밍 수치 재검증 통과"는 오기.
      실제로는 verify-timing-math.mjs가 기존 실패 4건을 보고(아래 미결 참조).

12. **역사 탭 금태환 이후 전구간 큐레이션** (2026-08-28, 11fa358~):
    - 요구: 1971-08 이후 하락 구간뿐 아니라 나머지 구간도 분석 가능하게.
      인터뷰로 확정: 큐레이션 추가(자유 구간 아님) · 전구간 빈틈없이 ·
      기존과 완전 동등 깊이. 계획 .omc/plans/2026-08-28-fiat-era-full-coverage.md
    - 구현: curatedEras.ts(5개 구간 선언: 1985-01 디스인플레 대세상승 /
      2013-05 최장 강세장 / 2020-01 팬데믹 붐-버스트 / 2021-11 긴축 약세장
      (−24.5%, 검출 기준 0.5%p 미달·회복 28개월) / 2024-03 AI 랠리 진행 중)
      + HistoryView EraView 통합 뷰모델(큐레이션을 에피소드 동형으로 물질화,
      런타임 실측 통계, kind 분기 배지·헤더·파란 밴드, 진행 중=dataEnd 바인딩)
      + 서사 5편·국면 30개·마커 25개(전부 번들 실측 data + 컨센서스 서사,
      근거 docs/research/fiat-era-expansions.md)
    - 노출 게이팅: 서사+연대기 등록 구간만 표시(점진 배포용) — 현재 5/5 활성,
      카드 12개. 커버리지 불변식(1971-08 이후 모든 달 ∈ 어느 상세 창)과
      큐레이션 구조 규칙을 tests/era-timelines.test.ts에 추가(테스트 173)
    - 문구 정합: "역사 7개 구간" 지칭 5곳을 "하락 구간 7개"로 명확화
      (guideContent 3·NowView 1·nowSignals 주석 1). 보호 인용 충돌 없음 확인
    - 검출 규칙·번들·build-history.mjs는 불변. 하락 7개의 "기계적 규칙"
      서사는 그대로 참
    - **검수 반영** (같은 날, 독립 리뷰어+기계 감사 이중 레인): data 수치
      약 200건 대조 불일치 0. 리뷰어 발견 22건 반영 — 상 1(2021-11 금
      서사의 실질금리 계열 혼용), 중 6(첫 달→두 달 귀속, 파월 1/4, 사후
      실질금리 라벨 8곳 명시+근거 문서 오기 정정, cause 과장 완화, 마커
      월 근거 2곳), 하 10(20% 이상, 종가 기준 −34%, CPI 7.7, 문체 2 등),
      경계 5. 부수 발견: 가이드·광기의 해부의 CPI 4.3% 예시가 스테일
      (번들 최신 3.4%) — 별도 과제로 분리 → 해소(2026-08-28): 4.3%는
      5월 프린트가 6월 라벨에 붙은 값이었음. 예시 시점(2026-07)과
      광기의 해부 기준일(2026-08-05)의 최신 발표는 모두 6월분이므로
      번들 실측 3.5%(2026-06=3.53, 6개월 전 2025-12=2.68→2.7% 유지)로
      정정 — 가이드 s1-cpi 본문·인용·해설, TIPS 절 인용·해설,
      광기의 해부 계기판(3.5%↑, mania 테스트 앵커 추가)

12. **홈 히어로 — 데이터 에디토리얼(A안) 구현** (2026-08-28, `HomeHero.tsx` 신설):
    - 배경: 사용자가 TradingView 랜딩(다크 풀블리드 사진 + 거대 타이포)을 보고
      "우리도 화려하게 못 꾸미나" 질문 → 디자인 캔버스로 3안 시안(A 데이터
      에디토리얼 라이트 / B 다크 관측소 / C 콕핏 확장) 제시, A 선택.
      캔버스: https://claude.ai/code/artifact/02a45cca-25a9-47cc-90fe-e9cef4297483
      (1페이지 = 선택안 A + 모바일 폴드, 2페이지 = B·C).
    - 설계 원칙: 사진 자산 없이 **번들의 126년 실질 총수익 곡선(주식·10년 국채·금,
      로그 눈금, 3개월 샘플)을 배경 아트**로. 라이선스·용량 문제 없음, 라이트/다크는
      토큰(stroke-[..] dark:stroke-[..])으로. 히어로는 풀블리드가 아니라 콘텍츠
      컨테이너 폭의 rounded-xl 밴드(좌측 레일과 충돌 회피). 콕핏(체제 카드·신호
      5종)은 그대로 아래에 이어짐 — 홈의 도구 역할 유지.
    - 문구: "126년의 시장을 먼저 읽습니다." + "예측이 아니라 위치 확인입니다. 역사
      데이터로 배우고, 정해 둔 규칙으로 투자합니다." (1부·신호 탭 원칙에서 가져옴,
      TradingView 문구 차용 없음). 연수·연도·4,353배는 데이터에서 동적 계산.
      CTA: 가이드북 시작/이어 읽기(진도 유무) + 현재 신호 보기. 로딩·에러 시에도
      문구는 먼저 렌더(곡선·범례만 데이터 도착 후).
    - 검증: 테스트 173, 빌드, eslint, 오버플로 0, wrap 신규 0, 데스크톱 라이트/다크·
      모바일 390 실캡처. 모바일은 아이브로 뒷부분(배당 재투자·물가 조정)을 sm+에서만
      표시, 하단 곡선 여백 축소로 첫 화면 안에 체제 카드 상단이 보이도록 조정.

## 3. 도메인 규약 (어기면 수익률이 틀어지는 것들)

- **엔진 하루 순서**: 이자 → 배당(ex-date) → 체결(매도 먼저) → 유입(i===0 포함
  월 적립) → 평가 → 낙폭 관측(5.5) → 결정(t+1 체결 큐 적재) → 연말 세금(+growth
  소급 보정) → 기록/리컨실. 룩어헤드는 코드상 불가능해야 한다.
- **낙폭 규칙**: 전일 관측이 다음 결정부터 반영, 여러 규칙 중 가장 깊은 하나만,
  회복 시 자동 해제. `cashTargetOverride` 시 시장 목표는 (1−effCash)/(1−cashTarget)
  비례 확대되어 밴드·리밸런싱·매수 배분 모두 유효 목표 기준.
- **-HIST 자산**: 월간 해상도 합성 지수(Shiller 파생), 일별 자산·크립토와 혼합
  금지(검증에서 차단). 크립토는 주식 거래일에만 샘플링.
- **테스트 앵커**: tests/*.test.ts 의 must-contain 문자열이 콘텐츠 수치를 고정한다.
  광기의 해부(−81.1%, 42.1, 44.2 등)·1부(4,285배, $146,614, 89.2% 등)·가이드
  must-terms·가나다 정렬. 콘텐츠 수정 시 반드시 함께 확인.

## 4. 가이드 콘텐츠 규약

- 구조: 1부 `TRADING_GUIDE_CHAPTERS` 7챕터(90분), 2부 `GUIDE_CHAPTERS` 9 STEP
  (124분). 분량 킥커는 GuideView에서 reduce로 동적 계산(하드코딩 금지).
  GUIDE_INTRO 문단의 "1부 90분 + 2부 124분"만 수동 — 분량 바꾸면 갱신.
- 도해 데이터 수치는 본문에 이미 있는 값만(새 수치 창작 금지, note에 가정 명시).
- 인용 보호: `quotes[].quote` 15건은 다른 파일 원문과 글자 단위 동기화.
- 용어 사전은 파트 내 절만 앵커 가능(교차 파트 점프 불가), localeCompare('ko')
  가나다 정렬을 테스트가 강제.
- 1부 수치는 `docs/guides/trading-discipline.md` 원문·`scripts/verify-timing-math.mjs`
  와 3중 동기화.

## 5. 미결·후보 과제 (착수 전 사용자 확인 권장)

- 검수에서 **기각/보류된 제안**: tg-m5 거치식vs적립식 쌍 막대(도해 컴포넌트가
  쌍 미지원+표와 중복), 자가진단 quad 지도, 워크벤치 stale 시 결과 흐림 처리.
- 홈 신호 카드 클릭 시 신호 탭의 **해당 카드로 앵커 이동**(현재는 탭 이동만).
- 홈 "이어서 읽기"가 가이드 탭의 **마지막 절로 직접 점프**(현재는 탭 이동 후
  가이드 내 버튼 한 번 더).
- 허용 잔존 wrap 2건(2026-08-25 교정에서 11→3→2): FactSet 과대치 나열·기준금리
  사전 정의의 22자+ 절 단위 괄호. 원하면 리라이트로 0건 가능.
- 신호 5종 외 후보(M2 갭)는 과거에 사용자 지시로 **롤백**됨 — 재제안 금지.
- ~~verify-timing-math.mjs 실패 4건~~ → **해소됨** (2026-08-27, "갱신+고정"
  방식): 순수 고정만으로는 재현 불가 판명(소스 교체가 과거 값도 미세 수정 —
  2026-05 절단 시 4280 vs 4285, 프로토콜 154 vs 161). 사용자 승인 하에
  현재 번들(2026-07)로 일괄 재계산·3중 갱신(4,285→4,353배, 161→157배,
  +6.84→+6.85%, +4.10→+4.08%, 27→28분의 1, 1,505→1,507개, 1,516→1,518개월,
  67.1→67.2%, 2.74→2.77%p) 후, timing-math.mjs·verify-timing-math.mjs
  양쪽에 `DOC_VINTAGE_END = '2026-07'` 핀을 넣어 이후 번들 갱신에도
  검증이 고정 시점 기준으로 유지되도록 함. md 부록 E에도 핀 설명 추가.
  이후 수치를 의도적으로 리프레시할 때는 핀 값과 앱·md·tests를 함께
  갱신할 것. 독립 재검증 전체 통과 확인.
- ~~연대기("흐름 따라가기") 직관성 개선~~ → **구현 완료(2026-08-25, 확장안 승인,
  §2-9 후속 2)**: 국면 확대+재정규화, lg+ 2열 sticky, 자동 실측, 이벤트 마커,
  일별 스트립. 모바일 UX 후속 개선은 §2-10.
- 역사 구간 상세의 "트레일링 vs 실현 선행 P/E" 차트 섹션은 실효성 부족으로
  **삭제됨(2026-08-25, 사용자 지시)**. 번들의 peTrail·peFwdReal 시리즈와 가이드
  s3-forward 절(교육 도해 포함)은 유지 — 재도입 시 데이터는 그대로 사용 가능.
- ~~1968=24.1 앵커 불일치~~ → **해결(2026-08-25, §2-9 후속)**: "에피소드 고점월
  실측 CAPE" 기준으로 통일(22.3/32.6/42.9, 사상 최고 44.2 별도 표기). 24.1은
  1966-01 CAPE 정점이 1968 에피소드에 잘못 귀속됐던 값.
- /ELI5 스킬: **해결(2026-08-25, 로컬 세션)** — 로컬 스킬 목록에 `eli5` 존재
  확인, `/eli5`로 호출 가능. claude.ai(클라우드) 세션에는 여전히 없을 수 있음.

## 6. 로컬 환경에서의 E2E (클라우드와 다른 점)

- 클라우드 스크래치의 검수 스크립트를 `scripts/e2e/`로 저장소에 편입했다:
  `staticserver.mjs`(dist 4175 서빙, /yf·/fred 404 — 라이브 폴백 차단),
  `e2e-fullaudit.mjs`(오버플로), `e2e-wrapaudit.mjs`(괄호 줄바꿈), README 참조.
- Playwright: `npm i -D playwright && npx playwright install chromium` 후 실행.
  크로미엄 경로를 직접 줄 때는 `CHROMIUM_PATH=... node scripts/e2e/...`.
- 백테스트 실주행 E2E는 네트워크 없이 -HIST 자산으로: 스크립트가
  `bt_strategies_v1`에 SPX-HIST 70/CASH 30(1960~1990)을 시드한다.

## 7. 기타 세션 맥락

- 스크래치 저장소 `khskblue-ui/-`의 PR #1(세션 초기 스냅숏)은 사용자 선택으로
  **머지 없이 닫음**. 정본은 이 저장소(portfolio-backtester)뿐이다. 다시 열거나
  같은 변경으로 새 PR을 만들지 말 것(사용자 명시 요청 시 제외).
- UI 시안 캔버스(3안, PC·모바일 6아트보드)는 위 아티팩트 URL에 보존.
- 광기의 해부 dataAsOf 2026-08-05, 신호 카드 수치는 항상 라이브가 우선이고
  가이드 인용 속 수치는 "예시" 면책 문구가 붙어 있다.
